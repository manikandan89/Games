
STEP 1 - I have included all the dependencies within the project. 
STEP 2 - Import the project to any Java IDE.
STEP 2 - The program can directly be executed by running the TestEngine.java
STEP 3 - There are no command line arguments